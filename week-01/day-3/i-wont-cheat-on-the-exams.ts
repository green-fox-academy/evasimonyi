'use strict';
export { };

// Create a program that writes this line 100 times:
// "I won't cheat on the exam!"

let text: string = 'I won\'t cheat on the exam!';

for (let i = 0; i < 100; i++) {
  console.log(text);
}
